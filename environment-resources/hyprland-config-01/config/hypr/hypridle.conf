general {
    lock_cmd = pidof swaylock || swaylock -f -i ~/.config/hypr/wallpapers/firewatch-01-blur-0x15.jpeg
}

listener {
    # 600 s = 10 min
    timeout = 600
    on-timeout = loginctl lock-session
}
